version: "3.2"
services:
  flask:
    build: .
    restart: always
    ports:
      - "8080:8080"
    environment:
      - DEPLOYMENT=production
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    volumes:
      - .:/home/myuser